import { ActiveDevicesPipe } from './active-devices.pipe';

describe('ActiveDevicesPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveDevicesPipe();
    expect(pipe).toBeTruthy();
  });
});
